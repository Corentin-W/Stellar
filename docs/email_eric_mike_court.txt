Bonjour Eric et Mike,

J'ai terminé la configuration de mon proxy pour Voyager RoboTarget.
Tout est prêt de mon côté, mais j'ai besoin de votre aide pour finaliser :

🚨 PROBLÈME : Le port TCP 5950 n'est pas accessible depuis l'extérieur
   Test : nc -z 185.228.120.120 5950 → timeout

Ce port est requis pour l'API Voyager (selon la documentation officielle).

ACTIONS REQUISES (URGENT) :

1️⃣ OUVRIR LE PORT 5950 dans le pare-feu Windows

   Méthode rapide (PowerShell en administrateur) :

   New-NetFirewallRule -DisplayName "Voyager API - Port 5950" -Direction Inbound -Protocol TCP -LocalPort 5950 -Action Allow -Profile Any

   OU via interface graphique :
   - Appuyez Windows+R → tapez "wf.msc" → Entrée
   - Clic droit "Inbound Rules" → "New Rule"
   - Type: Port → TCP → Port 5950 → Allow → Finish

2️⃣ VÉRIFIER LA VERSION DE VOYAGER
   - Voyager → Help → About
   - Doit être ≥ 2.3.13 (sinon télécharger : https://www.starkeeper.it/voyager/Voyager_Setup_2.3.13_64bit.zip)

3️⃣ ME COMMUNIQUER LE NUMÉRO DE SÉRIE VOYAGER
   - Voyager → Help → About → copier le Serial Number
   - Nécessaire pour que Leonardo génère la licence NDA

VÉRIFICATIONS APRÈS OUVERTURE DU PORT :

Sur le serveur, exécutez :
  netstat -an | findstr :5950

Vous devriez voir : "LISTENING" sur le port 5950

Si ce n'est pas le cas :
- Vérifiez que Voyager est configuré sur le port 5950 (Setup → Voyager → Application Server)
- Redémarrez Voyager

Une fois fait, confirmez-moi et j'effectuerai un test de connexion immédiatement !

J'ai créé un document avec toutes les instructions détaillées :
/Users/w/Herd/Stellar/docs/message_eric_mike_port_5950.md

Merci beaucoup !
Mikael
